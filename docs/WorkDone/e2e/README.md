# Docs E2E (source de vérité)

Ces docs décrivent l’exécution E2E Playwright **dans un conteneur runner dédié** (image officielle Ubuntu).

- Plan / décisions : `./playwright-plan.md`
- Tableau local vs CI (COMPOSE défaut + HOST fallback) : `./local-vs-ci.md`
- Convention des variables `E2E_*` : `./variables-convention.md`
